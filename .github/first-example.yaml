name: my first workflow 
on: [push,fork]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: welcome message
      run: echo "My first github action job"

    - name: list files
      run: ls

    - name: read files
      run: cat readme.md
      

